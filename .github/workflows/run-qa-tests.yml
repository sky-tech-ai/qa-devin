name: Run QA Tests

on:
  workflow_dispatch:
    inputs:
      tests:
        description: 'Comma-separated test names to run (leave empty for all tests)'
        required: false
        default: ''
        type: string
      url:
        description: 'Base URL for testing'
        required: false
        default: 'https://dev.app.usesky.ai/'
        type: string
      external_api_specs_url:
        description: 'URL for external API specs'
        required: false
        default: 'https://dev-api.app.usesky.ai/external-api/docs-json'
        type: string
      sample_pdf_url:
        description: 'URL for sample PDF document'
        required: false
        default: 'https://pdfobject.com/pdf/sample.pdf'
        type: string
      johndoejunior_zip_url:
        description: 'URL for John Doe Junior zip archive'
        required: false
        default: 'https://dev-assets.app.usesky.ai/previews/John_Doe_Junior.zip'
        type: string

jobs:
  run-qa-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run QA tests
        env:
          DEVIN_API_KEY: ${{ secrets.DEVIN_API_KEY }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
        run: |
          if [ -n "${{ inputs.tests }}" ]; then
            python3 run_qa_devin.py --tests "${{ inputs.tests }}" --url "${{ inputs.url }}" --external-api-specs-url "${{ inputs.external_api_specs_url }}" --sample-pdf-url "${{ inputs.sample_pdf_url }}" --johndoejunior-zip-url "${{ inputs.johndoejunior_zip_url }}"
          else
            python3 run_qa_devin.py --url "${{ inputs.url }}" --external-api-specs-url "${{ inputs.external_api_specs_url }}" --sample-pdf-url "${{ inputs.sample_pdf_url }}" --johndoejunior-zip-url "${{ inputs.johndoejunior_zip_url }}"
          fi

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qa-test-results
          path: |
            *.log
            *.json
          if-no-files-found: ignore
